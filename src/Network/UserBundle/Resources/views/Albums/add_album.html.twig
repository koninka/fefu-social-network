{% extends "NetworkWebBundle:Default:profile.base.html.twig" %}
{% trans_default_domain 'FOSUserBundle' %}
{% block right_content %}
    <div>
        <div>
            <form action="{{ path('user_add_album') }}" {{ form_enctype(form) }} method="POST">
                {{ form_widget(form) }}
                <div>
                    <input class="blue_button" type="submit" value="{{ 'album.add.submit'|trans }}" />
                </div>
            </form>
        </div>
    </div>
{% endblock %}
