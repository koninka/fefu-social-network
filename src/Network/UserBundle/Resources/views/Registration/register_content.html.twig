{% trans_default_domain 'FOSUserBundle' %}
{% block authorization %}
     <form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register register_form">
     {{ form_widget(form) }}
         {{ form_widget(form.gender, { 'attr': {'class': 'calendar'} }) }}
     <div>
     {#<input type="submit" value="{{ 'registration.submit'|trans }}" />#}
     <input type="submit" id="registration_button" {{ 'Регистрация'|trans }}" />
     </div>
     </form>
{% endblock %}


