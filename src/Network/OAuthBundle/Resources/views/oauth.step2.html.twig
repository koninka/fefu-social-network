{% extends "NetworkWebBundle:Default:auth.base.html.twig" %}
{% trans_default_domain 'FOSUserBundle' %}
{% block authorization %}
    <form action="{{ path('registration_step_2') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register register_form">
        {{ form_widget(form) }}
        {{ form_widget(form.gender, { 'attr': {'class': 'calendar'} }) }}
        <div>
            <input type="submit" class="btn btn-blue btn-auth-block" value="{{ 'registration.submit'|trans }}" />
        </div>
    </form>
    <a class="link block gray" href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
{% endblock %}
